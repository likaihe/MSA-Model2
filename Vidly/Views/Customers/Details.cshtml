@model Vidly.Models.Customer
@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@Model.Name</h2>
<ul>
    <li>@Model.MembershipeType.Name</li>
    @if (Model.Birthday != null)
    {
    <li>@Model.Birthday.Value.ToShortDateString()</li>
    }

</ul>

